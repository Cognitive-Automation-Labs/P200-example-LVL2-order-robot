tasks:
  do everything:
    command:
      - python
      - task.py
  do nothing:
    command:
      - python
      - print("hello world")

condaConfigFile: config/conda.yaml

artifactsDir: output

ignoreFiles:
  - .gitattributes
PATH:
  - .
PYTHONPATH:
  - .